#include <bits/stdc++.h>
//#include <iostream>
#include "src/algorithms.h"

using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    while(cin.){
        cin >> a >> b;
    }
//
//    while (getline(cin, line)) {
//        if (line == "") break;
//        input += " " + line;
//    }

    return 0;
}